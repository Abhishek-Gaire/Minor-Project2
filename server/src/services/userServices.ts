// interface User {
//     name: string;
//     email: string;
//   }

//   const users: User[] = [];

//   // Service to get all users (mocked for now)
//   export const getAllUsers = async (): Promise<User[]> => {
//     return users;
//   };

//   // Service to save a new user (mocked for now)
//   export const saveUser = async (name: string, email: string): Promise<User> => {
//     const newUser = { name, email };
//     users.push(newUser);
//     return newUser;
//   };
